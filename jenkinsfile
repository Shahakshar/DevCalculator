pipeline {
    agent any
    stages {
        stage('Clone Repository') {
            steps {
                git branch: 'main', url: 'https://github.com/Shahakshar/DevCalculator.git'
            }
        }
        stage('Build Docker Image') {
            steps {
                sh 'sudo docker build -t devcalculator .'
            }
        }
        stage('Push to Docker Hub') {
            steps {
                withDockerRegistry([credentialsId: '', url: '']) {
                    sh 'sudo docker tag devcalculator akdev6298/mycalculator:latest'
                    sh 'sudo docker push akdev6298/mycalculator:latest'
                }
            }
        }
    }
}

